export interface Frase {
    cli_id: string; // Contiene la frase
    cli_identificacion: string;
    cli_nombre: string; // Identificador la frase aleatoria
    cli_apellido: string;
    cli_correo: string; // URL que contiene el ID y permite recuperar la frase
    cli_telefono: string;
    cli_tp_doc_id: string;
  }